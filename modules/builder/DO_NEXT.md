# @m/builder — do next

- Span-accurate delete (per-statement spans from the parser) to replace the line-based `deleteNode`/
  `deleteEdge` heuristic; change-direction.
- Extend property coverage to `relabelNode` (bracket-wrap parity) and `connect`.
