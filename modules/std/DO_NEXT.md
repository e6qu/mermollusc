# @m/std — do next

- Add property-based tests (fast-check) for `Result` laws and `rectContains` invariants.
- Decide whether `event` in `LogRecord` should be generic so modules supply a closed union.
- Confirm `make build` (tsup) emits usable `dist` types for downstream modules.
